apiVersion: v1
kind: Service
metadata:
  name: homework-service
  namespace: homework  
  labels:
    app: homework-app
spec:
  type: ClusterIP
  selector:
    app: homework-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

